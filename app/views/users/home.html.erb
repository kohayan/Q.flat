<div class="mt-4">
	<% if @quizzes.blank? %>
		<h3 class="nothing">ユーザーをフォローしよう！</h3>
	<% else %>
		<% @quizzes.each do |quiz| %>
			<%= render 'quizzes/quiz', quiz: quiz %>
		<% end %>
	<% end %>
</div>