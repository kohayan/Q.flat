if (location.pathname.includes('/quizzes') || location.pathname.includes('/users')) {
	$(".quiz_<%= @quiz.id %>").html("<%= j(render 'quizzes/quiz', quiz: @quiz) %>")
}else{
	$(".quiz_<%= @quiz.id %>").html("<%= j(render 'quizzes/quiz_detail', quiz: @quiz) %>")
	location.reload();
}